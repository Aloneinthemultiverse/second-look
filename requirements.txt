# Pinned to the versions these results were produced with.
# Python 3.14.0
lightgbm==4.7.0
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.4.1
pytest==9.0.3
kagglehub==1.0.2

# Optional: enables the LLM path in investigator.py.
# Without it the deterministic template brief is used and everything still runs.
# anthropic>=0.40.0
